apply plugin: "war"

bootWar {
    mainClassName = "com.landon.hotel.RoomserviceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
